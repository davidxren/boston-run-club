# First message for the Claude Code session

Paste exactly this after opening the repo folder.

---

Read CLAUDE.md, then read docs/build-session-v2.md in full. It is the contract for this session.

Then:

1. Run gate G0. If it fails, print the exact gate failure string, write the report, stop.
2. Do D1 through D9 in order. One commit per deliverable, message `D<n>: <what>`, no diff outside the current deliverable.
3. Stop and ask at every "ask before" item. Do not stub past a gate.
4. Seed files are already in `seed/`. Load them, never retype or edit them.
5. Finish with the report in the exact format and order in the contract. Nothing before item 1.

Before writing any code, list any statement in the contract you read as ambiguous or contradictory, with the reading you intend to use. Then start D1.
